{
  "name": "the-leather-store",
  "version": "1.0.0",
  "private": true,
  "description": "Storefront website for The Leather Store — static HTML/CSS/JS, no build step.",
  "scripts": {
    "dev": "npx --yes serve . --listen 5173",
    "start": "npx --yes serve . --listen 5173",
    "serve:python": "python -m http.server 5173"
  },
  "keywords": ["leather", "storefront", "static-site"],
  "license": "UNLICENSED",
  "browserslist": ["defaults", "iOS >= 13", "Safari >= 13", "Android >= 8"]
}
